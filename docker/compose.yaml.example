# The v1 (TypeScript server) compose example was removed with the v1 codebase.
# Sonarly v2 (Go) is the only server. Use docker/compose.v2.yaml.example —
# copy it to compose.yaml (gitignored) and adjust.
#
# For local development, see docs/development.md (go run + pnpm dev).
