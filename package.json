{
  "name": "sonarly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r --no-bail test",
    "lint": "echo 'Linting not configured'"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "pnpm": {
    "overrides": {
      "tar": "^7.5.21",
      "vite": "^6.4.3",
      "esbuild": "^0.25.0",
      "@mapbox/node-pre-gyp": "^2.0.2"
    }
  },
  "packageManager": "pnpm@9.0.0"
}
