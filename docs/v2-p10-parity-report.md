# Sonarly v2 — P10 parity & cutover evaluation report

Generated by `go test ./testparity/` on 2026-09-26 10:41.

- **Cases**: 94 total — 94 passed, 0 failed
- **Suite wall time**: 5s
- **Verdict**: READY FOR CUTOVER (no blockers)

## Method

`testparity` seeds a temp library (12 songs / 3 albums / 4 artists, mp3+flac+ogg+m4a, multi-genre, one explicit, one with embedded art, lyrics, multi-disc, track totals) with the `internal/audio` mutagen writer, boots the **v1 TypeScript server** from the main checkout, scans, then snapshots the SQLite DB and boots the **v2 Go server** on the copy (same library dir, same `SESSION_SECRET`, so the v1-created admin user and its Subsonic secret box work on both). One deterministic request script is replayed against both; v2 also runs its boot-time scan first (the scanner is file-read-only; this is the 'both do a post-import scan pass' option from the P10 brief). The DB snapshot is taken between the script's read phase and write phase, so write round-trips (favorites, ratings, scrobbles, playlists, bookmarks, star) replay from identical state on both servers. Created-entity ids and share tokens are server-generated uuids: the harness maps v1 values onto v2 counterparts before diffing; write-time timestamps are canonicalized on both sides. **Any delta not covered by a commented rule in `parity_norm.go` is a BLOCKER.**

## Case results

| Group | Case | Result | Accepted deltas |
|---|---|---|---|
| native | setup-status | PASS | - |
| native | login | PASS | - |
| native | me | PASS | - |
| native | preferences | PASS | - |
| native | songs-list | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry |
| native | song-detail-explicit-art | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry |
| native | song-detail-lyrics | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry |
| native | albums-list | PASS | - |
| native | album-detail | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry |
| native | artists-list | PASS | - |
| native | artist-detail | PASS | P10-v2-artist-detail-songs |
| native | artist-songs | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry, P10-v1-artist-songs-trim |
| native | genres-list | PASS | - |
| native | genres-tree | PASS | - |
| native | genre-albums | PASS | P10-v1-genre-albums-trim |
| native | years | PASS | - |
| native | cover-art-embedded | PASS | - |
| native | search-coil | PASS | P10-search-LIKE-vs-FTS |
| native | search-sunset-exact | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-search-trim |
| native | home | PASS | P8-home-genres-ranked-objects |
| native | statistics-me-baseline | PASS | - |
| native | players-baseline | PASS | - |
| oss | ping | PASS | E2-serverVersion-buildinfo |
| oss | ping-bad-token | PASS | E2-serverVersion-buildinfo |
| oss | getLicense | PASS | E2-serverVersion-buildinfo |
| oss | getUser | PASS | E2-serverVersion-buildinfo, S5-getUser-folder-scoped-ids |
| oss | getMusicFolders | PASS | E2-serverVersion-buildinfo, B1-musicFolders-real-ids |
| oss | getIndexes | PASS | E2-serverVersion-buildinfo, B2-lastModified-content-derived |
| oss | getArtists | PASS | E2-serverVersion-buildinfo |
| oss | getArtist | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | getAlbum | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | getSong | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | search3-coil | PASS | - |
| oss | getGenres | PASS | E2-serverVersion-buildinfo |
| oss | getAlbumList-alphabetical | PASS | - |
| oss | getAlbumList-newest | PASS | - |
| oss | getSongsByGenre-electronic | PASS | - |
| oss | getSongsByGenre-downtempo | PASS | - |
| oss | getTopSongs | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | getAlbumInfo2 | PASS | E2-serverVersion-buildinfo |
| oss | getArtistInfo2 | PASS | E2-serverVersion-buildinfo |
| oss | getSimilarSongs2 | PASS | E2-serverVersion-buildinfo |
| oss | getRandomSongs | PASS | - |
| oss | getLyrics-by-id | PASS | P10-v1-lyrics-first-entry |
| oss | getLyrics-by-artist-title | PASS | P10-v1-lyrics-first-entry |
| oss | stream-direct-range | PASS | - |
| oss | stream-direct-full | PASS | - |
| oss | stream-transcode | PASS | - |
| oss | download | PASS | - |
| oss | getCoverArt-song-embedded | PASS | - |
| oss | getCoverArt-album | PASS | - |
| oss | getAvatar | PASS | R10-getAvatar-explicit-404 |
| oss | radio-podcast-stubs | PASS | E2-serverVersion-buildinfo |
| native | favorites-song | PASS | SPEC-interactions-id-keys |
| native | ratings-song | PASS | SPEC-interactions-id-keys |
| native | favorites-album | PASS | SPEC-interactions-id-keys |
| native | favorites-artist | PASS | SPEC-interactions-id-keys |
| native | song-detail-after-interactions | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry |
| native | song-detail-after-rating | PASS | FF4a-song-drops-filePath-checksum, P10-v1-native-song-trim, SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-lyrics-first-entry |
| native | playlist-create | PASS | P10-v1-playlist-write-response |
| native | playlist-get | PASS | SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-playlist-detail-trim |
| native | playlist-list | PASS | - |
| native | playlist-update | PASS | P10-v1-playlist-write-response |
| native | playlist-get-after-update | PASS | SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-playlist-detail-trim |
| native | playlist-share-link | PASS | - |
| native | playlist-anonymous-share | PASS | SPEC-duration-integer-seconds, P10-v1-scan-stamps-files, P10-v1-playlist-detail-trim |
| native | scrobble-detailed | PASS | - |
| native | scrobble-minimal | PASS | - |
| native | statistics-me-after-scrobbles | PASS | P10-v1-half-star-histogram-drop |
| oss | star-multi | PASS | E2-serverVersion-buildinfo |
| oss | getStarred2 | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | getStarred | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | unstar-artist | PASS | E2-serverVersion-buildinfo |
| oss | getStarred2-after-unstar | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | setRating | PASS | E2-serverVersion-buildinfo |
| oss | getSong-after-rating | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | getAlbum-starred | PASS | E2-serverVersion-buildinfo, P10-v1-scan-stamps-files |
| oss | scrobble | PASS | E2-serverVersion-buildinfo |
| oss | scrobble-submission-false-noop | PASS | E2-serverVersion-buildinfo |
| oss | createPlaylist | PASS | E2-serverVersion-buildinfo, P10-adapter-playlist-coverArt |
| oss | updatePlaylist | PASS | E2-serverVersion-buildinfo |
| oss | getPlaylist-adapter | PASS | E2-serverVersion-buildinfo, P10-adapter-playlist-coverArt |
| oss | getPlaylists-adapter | PASS | E2-serverVersion-buildinfo, P10-adapter-playlist-coverArt |
| oss | deletePlaylist-adapter | PASS | E2-serverVersion-buildinfo |
| oss | getPlaylists-after-delete | PASS | E2-serverVersion-buildinfo, P10-adapter-playlist-coverArt |
| oss | stream-for-now-playing | PASS | - |
| oss | getNowPlaying | PASS | P8-players-keyed-by-device |
| native | players-after-streams | PASS | P8-players-keyed-by-device |
| oss | createBookmark | PASS | E2-serverVersion-buildinfo |
| oss | getBookmarks | PASS | E2-serverVersion-buildinfo |
| oss | deleteBookmark | PASS | E2-serverVersion-buildinfo |
| oss | getBookmarks-after-delete | PASS | E2-serverVersion-buildinfo |
| native | playlist-delete | PASS | - |
| native | playlist-list-after-delete | PASS | - |

## Accepted deltas (normalized, with justification)

- **B1-musicFolders-real-ids** (1 cases) —  quirks B1: v1 folder ids are positional indexes; v2 uses real library ids
- **B2-lastModified-content-derived** (1 cases) —  quirks B2: v1 lastModified is Date.now() per request; v2 derives it from content
- **E2-serverVersion-buildinfo** (36 cases) —  quirks E2: v2 serverVersion comes from build info instead of v1's hardcoded 0.1.0
- **FF4a-song-drops-filePath-checksum** (8 cases) —  frontend audit FF4(a): v2 Song has no filePath/checksum
- **P10-adapter-playlist-coverArt** (4 cases) —  v2 adapter playlists carry a computed coverArt (first entry's album art); v1's adapter never emitted playlist coverArt — additive Subsonic-standard field, normalized for comparison
- **P10-search-LIKE-vs-FTS** (1 cases) —  P10 brief: v1 search is LIKE-based, v2 is FTS5-based; cases compare sorted id sets + intersection ratio instead of order/exact hits. Known engine-scope gap: v1's native song LIKE matches title/artist/album names while v2's FTS song index (P8 DR-2) covers titles, so the native songs category floors at 0.3
- **P10-v1-artist-songs-trim** (1 cases) —  v1's /api/artists/:id/songs projection omits albumArtistName, albumName and artistName (unlike its /api/songs list); v2 returns the standard song card — trimmed for comparison
- **P10-v1-genre-albums-trim** (1 cases) —  v1's /api/genres/:id/albums projection omits the album explicit flag, genreId, shownSongCount, totalSongCount and starred; v2's album card includes them — trimmed for comparison
- **P10-v1-half-star-histogram-drop** (1 cases) —  v1's ratingDistribution histogram looks up exact float ratings on the whole-star ladder [1..5] — half-star ratings (4.5) are dropped entirely; v2 folds them into the integer bucket (int(4.5)=4) — buckets where v1 reports 0 tolerate v2's half-star counts
- **P10-v1-lyrics-first-entry** (7 cases) —  v1's reader takes common.lyrics[0] — the SYLT frame on SYLT-bearing files — and drops the plain USLT text (tags/reader.ts extractPlainLyrics; verified against music-metadata's parse in P10); v2 reads both — lyrics keys trimmed on both sides
- **P10-v1-lyrics-first-entry** (2 cases) —  v1's reader takes common.lyrics[0], which is the SYLT frame on SYLT-bearing files — plain USLT lyrics are dropped (v1 quirk, tags/reader.ts extractPlainLyrics); v2 reads both — accepted when v1 is empty and v2 is not
- **P10-v1-native-song-trim** (8 cases) —  v1's native /api/songs list+detail share one trimmed rowToSong projection (no bitRate/sampleRate/lyrics-adjacent technical fields); v2's spec'd Song DTO is a documented superset — trimmed on both sides, all shared fields still compared
- **P10-v1-playlist-detail-trim** (3 cases) —  v1's native playlist detail omits ownerUsername (the list projection has it) and includes a flat songIds list alongside entries; v2's Detail carries ownerUsername and no songIds — both trimmed for comparison
- **P10-v1-playlist-write-response** (2 cases) —  v1's native playlist create/update responses omit the entries list, songCount, ownerUsername, shares, and carry a flat songIds list v2's spec'd Detail replaces with entries (api/openapi.yaml PlaylistDetail has no songIds); v2 returns the full Detail — trimmed in write responses, compared in detail cases
- **P10-v1-scan-stamps-files** (20 cases) —  v1's scan writes album cover art into the audio files (scanner.ts syncSongCoverWithAlbum), moving mtimes to scan time; v2's scanner is file-read-only — mtime-derived fields (native mtime, /rest created) are canonicalized, cover-art bytes still compared exactly
- **P10-v1-search-trim** (1 cases) —  v1's native search song hits carry no genres array and no syncedLyrics (search/routes.ts attaches artist entries only); v2's search DTO carries both — trimmed on both sides
- **P10-v2-artist-detail-songs** (1 cases) —  v2's artist detail adds a songs array (v1's detail carries albums only, docs/api.md) — v2 superset trimmed for comparison
- **P8-home-genres-ranked-objects** (1 cases) —  v2 home genres are ranked {name, songCount} objects scoped to in-scope songs (documented deviation in internal/modules/home/service.go); v1 returned the alphabetical name union — compared as name sets
- **P8-players-keyed-by-device** (2 cases) —  v2's player tracker keys by user+device (P8), keeping one entry per client id; v1's tracker keys by user only and overwrites — compared filtered to the deterministic ParityPlayer entry
- **R10-getAvatar-explicit-404** (1 cases) —  R10-getAvatar-explicit-404: v1 had no getAvatar (Fastify default 404 JSON); v2 answers a plain-text 404 (quirks R10)
- **S5-getUser-folder-scoped-ids** (1 cases) —  quirks S5: v1 getUser.folder is hardcoded ["0"]; v2 returns real scoped library ids
- **SPEC-duration-integer-seconds** (11 cases) —  v2 spec (api/openapi.yaml Song.duration): integer seconds; v1 stored music-metadata float seconds (3.134…) — v1 value truncated toward zero for the comparison
- **SPEC-interactions-id-keys** (4 cases) —  v2 spec (api/openapi.yaml setFavorite/setRating): per-type id keys instead of v1's {entityType, entityId}; same user_* junction semantics — harness drives each contract and compares resulting state

## Blockers

None found.

## Blocker summary

None — all compared surfaces match after normalization. The P10 share-link/visibility blocker was resolved by the strict-v1-parity decision: the share-token lifecycle is independent of visibility (native share-link/delete touch only the token; native visibility PUTs never clear it), the Subsonic updatePlaylist re-derives the token from the resolved visibility like v1, and the native anonymous metadata view keeps v1's matching-token-grants-view rule while streaming/content grants and the adapter stay on link+token (internal/modules/playlists/doc.go).

## Performance smoke (informational)

Sequential fetch of 200 song details over 12 songs:

| Server | Wall time | Per request |
|---|---|---|
| v1 (Node/TS) | 153.57403ms | 767.87µs |
| v2 (Go) | 81.340034ms | 406.7µs |

v2/v1 wall-time ratio: **0.53x**.

## Environment notes

- v1: main checkout `packages/server` via `pnpm exec tsx src/index.ts`, temp DATA_DIR, scan/ingest/artist-image intervals 0
- v2: `go build ./cmd/sonarly` binary, `SONARLY_SCAN_INTERVAL_MINUTES=0` (boot scan still runs once, read-only re: files)
- Native bookmarks: v1 has no native bookmark routes (only `/rest/*`); bookmark parity is covered under OpenSubsonic, v2's native `/api/bookmarks` is an addition, not a delta
- Seed tag writes go through `internal/audio.MutagenWriter` (python3+mutagen, the production path); the one embedded-art file gets its APIC frame via the same mechanism
