# Sonarly

## Local development requirements

The server uses Python's [Mutagen](https://mutagen.readthedocs.io/) library to
write audio tags. Make sure `python3` and `mutagen` are installed:

```bash
pip3 install mutagen
```

